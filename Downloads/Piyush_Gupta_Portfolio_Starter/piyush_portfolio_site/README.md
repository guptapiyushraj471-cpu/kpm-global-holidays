# Piyush Gupta – Portfolio Site

A clean, responsive portfolio for AI + Automation projects. Built with **HTML + CSS + vanilla JS** (no frameworks).

## 🧩 Structure
```
piyush_portfolio_site/
├─ index.html
├─ assets/
│  ├─ css/style.css
│  ├─ js/main.js
│  └─ img/logo.svg
└─ data/
   └─ projects.json
```

## 🚀 Run locally
Open `index.html` directly, or serve the folder:
```bash
# Python 3
python -m http.server 8080
```

Then visit: http://localhost:8080

## 🔧 Customize
- Replace links (`guptapiyush471-cpu`, LinkedIn, email) in `index.html`.
- Update project entries in `data/projects.json`.
- Drop your resume at `resume/Piyush_Gupta_Resume.pdf` and fix the link.
- Tweak styles in `assets/css/style.css`.

## 📦 Deploy
- **GitHub Pages**: push to a repo and enable Pages → root/`main`.
- **Netlify/Vercel**: drag-drop the folder or connect the repo.

## © License
MIT
